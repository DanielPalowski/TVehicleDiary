<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Repair History Hub</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
            --secondary-gradient: linear-gradient(135deg, #ff6b6b 0%, #ff8e53 100%);
            --tertiary-gradient: linear-gradient(135deg, #0bd1d1 0%, #00aaff 100%);
            --card-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            --card-hover-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        html {
            font-size: 14px;
        }

        @@media (min-width: 768px) {
            html

        {
            font-size: 16px;
        }

        }

        .btn:focus, .btn:active:focus, .btn-link.nav-link:focus, .form-control:focus, .form-check-input:focus {
            box-shadow: 0 0 0 0.1rem white, 0 0 0 0.25rem #258cfb;
        }

        html {
            position: relative;
            min-height: 100%;
        }

        body {
            background-color: #f8f9fa;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        .text-gradient {
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .card-hub {
            border: none;
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.3s ease;
            height: 100%;
            box-shadow: var(--card-shadow);
        }

            .card-hub:hover {
                transform: translateY(-8px);
                box-shadow: var(--card-hover-shadow);
            }

        .card-repairs {
            background: var(--primary-gradient);
        }

        .card-upgrades {
            background: var(--secondary-gradient);
        }

        .card-diagnostics {
            background: var(--tertiary-gradient);
        }

        .card-icon {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            opacity: 0.9;
        }

        .btn-modern {
            border-radius: 50px;
            padding: 12px 30px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: none;
        }

            .btn-modern:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
            }

        .hub-section {
            padding: 60px 0;
        }

        .card-content {
            padding: 2.5rem;
            color: white;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .hero-section {
            background: linear-gradient(135deg, rgba(37, 99, 235, 0.1) 0%, rgba(30, 64, 175, 0.1) 100%);
            border-radius: 16px;
            padding: 3rem 2rem;
            margin-bottom: 3rem;
            text-align: center;
        }

        .stat-card {
            background: white;
            border-radius: 12px;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
        }

            .stat-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
            }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: #6c757d;
            font-weight: 500;
        }

        .feature-icon {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            background: rgba(37, 99, 235, 0.1);
            color: #2563eb;
            font-size: 1.8rem;
        }

        /* Additional styles to match your CSS */
        .btn-primary {
            background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
            border: none;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
        }

            .btn-primary:hover {
                background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
                transform: translateY(-2px);
                box-shadow: 0 4px 8px rgba(37, 99, 235, 0.3);
            }

        .feature-card {
            transition: transform 0.25s ease, box-shadow 0.25s ease;
        }

            .feature-card:hover {
                transform: translateY(-6px);
                box-shadow: 0 1rem 2rem rgba(0,0,0,0.15);
            }
    </style>
</head>
<body>
    <div class="container py-5">
        <!-- Hero Section -->
        <div class="hero-section">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <h1 class="display-5 fw-bold text-gradient mb-3">Welcome to Repair History Hub</h1>
                    <p class="lead mb-4">Complete overview of your vehicle's history in one place. Track repairs, upgrades, and diagnostic records for better vehicle management and maintenance.</p>

                    <div class="d-flex justify-content-center gap-3 flex-wrap">
                        <a asp-controller="RepairVehicle" asp-action="Index" asp-route-vehicleIDRoute="@Model.vehicleId"
                           class="btn btn-primary btn-modern">
                            <i class="fas fa-history me-2"></i>Repair History
                        </a>
                        <button class="btn btn-outline-primary btn-modern">
                            <i class="fas fa-download me-2"></i>Download Report
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Statistics Section -->
        <div class="row mb-5">
            <div class="col-md-4 mb-4">
                <div class="stat-card feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-tools"></i>
                    </div>
                    <div class="stat-number">X</div>
                    <div class="stat-label">Total Repairs</div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="stat-card feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                    <div class="stat-number">X</div>
                    <div class="stat-label">Diagnostics</div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="stat-card feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <div class="stat-number">X</div>
                    <div class="stat-label">Upgrades</div>
                </div>
            </div>
        </div>

        <!-- Hub Cards Section -->
        <div class="hub-section">
            <h2 class="text-center mb-5 fw-bold">Explore Your Vehicle's History</h2>

            <div class="row g-4">
                <!-- Vehicle Repairs Card -->
                <div class="col-md-4">
                    <div class="card-hub card-repairs">
                        <div class="card-content">
                            <div class="text-center mb-4">
                                <i class="card-icon fas fa-tools"></i>
                                <h3 class="fw-bold">Vehicle Repairs</h3>
                            </div>
                            <p class="flex-grow-1">View complete history of all repairs performed on your vehicle including dates, services, and costs.</p>
                            <div class="text-center mt-4">
                                <a asp-controller="RepairVehicle" asp-action="Index" asp-route-vehicleIDRoute="@Model.vehicleId"
                                   class="btn btn-light btn-modern">
                                    Explore Repairs <i class="fas fa-arrow-right ms-2"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Upgrades Card -->
                <div class="col-md-4">
                    <div class="card-hub card-upgrades">
                        <div class="card-content">
                            <div class="text-center mb-4">
                                <i class="card-icon fas fa-tachometer-alt"></i>
                                <h3 class="fw-bold">Upgrades</h3>
                            </div>
                            <p class="flex-grow-1">Track performance upgrades and modifications made to your vehicle to enhance its capabilities and features.</p>
                            <div class="text-center mt-4">
                                <a href="#" class="btn btn-light btn-modern disabled" aria-disabled="true">
                                    Coming Soon <i class="fas fa-clock ms-2"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Diagnostics Card -->
                <div class="col-md-4">
                    <div class="card-hub card-diagnostics">
                        <div class="card-content">
                            <div class="text-center mb-4">
                                <i class="card-icon fas fa-stethoscope"></i>
                                <h3 class="fw-bold">Diagnostics</h3>
                            </div>
                            <p class="flex-grow-1">Access diagnostic reports and error codes to understand your vehicle's health and performance status.</p>
                            <div class="text-center mt-4">
                                <a href="#" class="btn btn-light btn-modern disabled" aria-disabled="true">
                                    Coming Soon <i class="fas fa-clock ms-2"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>